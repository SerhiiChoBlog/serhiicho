{{ seriesItem = post.Series.len() > 0 ? post.Series[0] : nil }}

@if(hasValue(seriesItem.PostsCount))
    <a
        href="/series/{{ seriesItem.Slug }}"
        class="opacity-60 font-bold"
    >
        Part {{ seriesItem.Pivot.Part }} of {{ seriesItem.PostsCount }}
    </a>
@end